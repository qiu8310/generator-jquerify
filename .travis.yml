language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-jquerify' ]; then cd .. && eval "mv $currentfolder generator-jquerify" && cd generator-jquerify; fi
